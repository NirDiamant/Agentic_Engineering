# log: {{PROJECT_NAME}}

<!-- The captain's log. Starts almost empty. That's correct. It grows every time the
     model surprises you or you make the same mistake twice. Don't pre-fill it with
     documentation; it earns its content as you build. When an entry here would have
     saved you an hour, it earned its place. Append-only: a new entry goes at the
     bottom of its section, and nothing already written above it is edited away. -->

Decisions, gotchas, and the claims the existing docs get wrong. Two lines each. A notebook, not documentation.

## How to use

- Discovered a fact that isn't obvious from the code (a library quirk, a decision and its reason, a gotcha that bit you)? Write it here in one or two lines.
- Something behaving in a way you didn't expect? Check here first. The answer might already be written down.
- If an entry grows into a real rule, move it to `CONVENTIONS.md` or `CLAUDE.md`, where it's enforced instead of just remembered.

## Decisions

- _(empty, record project decisions here as you make them, with the reason attached)_

## Gotchas

- _(empty, record the surprises here: what happened, what the real cause was, one or two lines each)_

## Contradictions in your existing docs

<!-- Claims this project's own docs make about the code that the code disproves, one line each:
     the claim, then the file and line that disproves it. A contradiction is a statement about the
     code that is false on this tree, not a difference of style or taste. The doc that carries the
     claim is not edited; the correction is recorded here. Zero entries is an honest and common
     answer. -->

- _(empty, nothing in the existing docs contradicted the code)_
