# TECH_STACK: {{PROJECT_NAME}}

<!-- fill: the technologies this project uses and the REASON behind each one. The AI
     reads this so it generates code for the stack you actually chose, the same way
     every session, instead of reaching for whatever it saw most in training.
     Delete these comments as you fill it in. -->

The technologies this project uses and the reason behind each one.

## Stack

<!-- fill: one line per layer. Name the exact tool, not the category. Drop the layers you don't have. -->

- **Language:** {{...}}
- **Framework:** {{...}}
- **Styling:** {{...}}
- **Database:** {{...}}
- **Auth:** {{...}}
- **Hosting:** {{...}}
- **Package manager:** {{...}}

## Why each choice

<!-- fill: one short reason per choice. The reason is what keeps the choice stable: a
     stack line without a reason gets "helpfully" swapped the first time the model has
     a different idea. "It's what I know" is a valid reason. -->

- **{{Tool}}**: {{the reason you picked it, in one or two sentences}}.
- **{{Tool}}**: {{...}}
- **{{Tool}}**: {{...}}

## Constraints

<!-- fill: the hard lines. What must never be mixed, added, or swapped without a
     conversation first. -->

- One package manager only: {{name it}}. Mixing them corrupts the lockfile.
- No new dependency without a reason written down here.
- {{Any naming or boundary constraint between layers, e.g. database snake_case vs app camelCase.}}
